spring.application.name=laweact-server
spring.datasource.url=jdbc:postgresql://localhost:5432/laweact
spring.datasource.username=laweact
spring.datasource.password=laweact
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration,classpath:db/seed
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
jwt.secret=4EzTRvc+xQKNaYxc5yor+2BfmGWdQRLbAgX/NsVkuhO+A9Hmuo7vCRPL0TOMIyhTmZi7hx+Y/9XwFV+hcZ8IjQ==
jwt.access-token-expiration-ms=3600000
jwt.refresh-token-expiration-ms=604800000
management.endpoints.web.exposure.include=health,info
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# Email Configuration
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:authsofttesteemail@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:gidplbjludvzcwiu}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
laweact.mail.from=${SPRING_MAIL_USERNAME:authsofttesteemail@gmail.com}

# Secrets locais (gitignored) — application-secrets.properties na pasta server/
spring.config.import=optional:file:./application-secrets.properties

# AWS S3 — keys em application-secrets.properties (não no GitHub)
aws.s3.enabled=true
aws.s3.bucket=daniel-juridico
aws.s3.region=us-east-1
